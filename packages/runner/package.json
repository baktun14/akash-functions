{
  "name": "runner",
  "version": "2.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:image": "docker build -t akash-fns/runner:dev .",
    "smoke": "docker run --rm -p 3000:3000 -e FUNCTION_ID -e INITIAL_VERSION_ID -e BACKEND_BASE_URL -e RUNNER_TOKEN akash-fns/runner:dev"
  }
}
